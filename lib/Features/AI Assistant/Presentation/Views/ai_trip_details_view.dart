import 'package:flutter/material.dart';

class AITripDetailsView extends StatelessWidget {
  const AITripDetailsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
